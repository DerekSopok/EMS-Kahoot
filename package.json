{
  "name": "kahootclone",
  "version": "1.0.0",
  "description": "A kahoot website clone",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "test": "node --test",
    "migrate": "node -e \"console.log('Migrations are archived; no database action performed.')\"",
    "seed": "node -e \"console.log('Seeds are archived; no database action performed.')\"",
    "reset-db": "node -e \"console.log('Reset DB has been removed; no database action performed.')\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.16.3",
    "moment": "^2.22.1",
    "node-fetch": "^2.7.0",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "expect": "^23.0.1"
  }
}
